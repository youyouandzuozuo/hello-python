# 学习日志

> 每次收工前花 2 分钟填一条。它是两台电脑接续的关键，也是 8 周后复盘的唯一依据。

---

## 2026-09-08 | 第0周 | 投入 30 分钟 | 机器：单位

- 看了：无（今天只做环境）
- 敲了：hello.py
- 卡住：无
- 状态：已解决
- 环境：系统 Python 3.14.7，venv 已重建，pip 已换清华源，Git 身份已配
- 接续：HTTPS 改为 **SSH**（ed25519 密钥，两台各一把），`git pull` / `git push` 均免密
- 接续确认：家里这台已 pull 到本条（单位→家里 通）
- 下次从哪开始：第1周 A 段 —— 0.2 全课程内容导学 → 1.1 程序设计基本方法

---

## 2026-09-08 | 第1周 | 投入 60+ 分钟 | 机器：单位

- 看了：0.2 全课程内容导学、1.1 程序设计基本方法
- 理解了：IPO —— 输入(Input) → 处理(Process) → 输出(Output)。以后看不懂的代码，先问「它的 I 是什么、O 是什么」
- 敲了：hello.py 在 VS Code 终端跑通（venv 激活，提示符前有 `(.venv)`）
- 卡住：PowerShell 里跑 `curl -H ... -d ...` 报 `ParameterBindingException`
- 状态：已解决 —— **PowerShell 的 curl 是 `Invoke-WebRequest` 的别名，不是真 curl**，要写 `curl.exe` 才行。改用 Python 标准库脚本验证，更省事
- 环境：VS Code 已信任 `D:\cs-learning`（Python 扩展生效，语法高亮正常）
- 钥匙：DeepSeek key 存 `.env`，代码里用 `os.getenv()` 读，不写进 .py、不进 git
- 验证：`test_deepseek.py` 跑通，DeepSeek 回「成功」。记住 **401 = key 错，402 = 余额不足**（两者别搞混）
- 下次从哪开始：A 段 1.3 温度转换 → 1.4 语法元素分析；B 段手敲温度转换 5 遍（不许用 if 和循环）

---

## 2026-09-09 | 第1周 | 投入 90+ 分钟 | 机器：单位

- 看了：1.3 实例 1：温度转换、1.4 Python 程序语法元素分析
- 敲了：`week01/TempConvert.py`（照抄教材完整版，跑通）、`week01/eval_demo.py`（eval 试验）
- 卡住：`eval("a:1")` 报 `SyntaxError: invalid syntax`
- 状态：已解决 —— **eval 只接受「表达式」（有值的东西），不接受「语句」（赋值、判断、循环这些动作）**。`a:1` 既不是表达式也不是语句，所以直接语法错。eval 在温度转换里的唯一作用：把字符串 `"82"` 变成数字 `82`，这样才能参与计算
- 环境：VS Code 里 IDLE 的对应方式搞明白了（`>>>` = 终端敲 `python`；F5 = 右上角 ▶）；**第 1 周别按 F5 调试器**
- 其他：新建 `DAILY.md` 每日清单（开工 / 碎片 / 整块 / 收工 + 第 1 周进度 + 周末自查）
- 下次从哪开始：B 段第 2 遍 —— 输入华氏输出摄氏（改数字和公式，不许加 if / 循环）

---

## 2026-09-09 晚 | 第1周 | 投入 40 分钟 | 机器：家

- 看了：无（晚上只敲）
- 敲了：`week01/TempConvert_v2.py` —— **脱稿重敲**，不看书，凭记忆敲完并跑通
- 卡住：栽了三个坑，都记进 `DAILY.md` 第七节「我踩过的坑」了
  1. `.format()` 甩到 `print` 括号外 → `AttributeError: 'NoneType' object has no attribute 'format'`
  2. 切片写成小括号、中间用逗号：`TempStr(0,-1)` → 应写 `TempStr[0:-1]`
  3. `if` / `elif` / `else` 后面漏冒号
- 状态：已解决
- 下次从哪开始：第 3 遍 —— 开尔文转摄氏（改字符串 `'F','f'` → `'K','k'`，改公式 `(x-32)/1.8` → `x-273.15`）

---

## 2026-09-10 | 第1周 | 投入 约 90 分钟 | 机器：单位

- 看了：无（A 段 0.2 / 1.1 / 1.3 / 1.4 已全部看完，今天全在敲和查）
- 敲了：
  - `week01/TempConvert_v3.py` —— 第 3 遍，开尔文转摄氏（第一个真变式，自己想明白公式含义才改对）
  - `week01/TempConvert_v4.py` —— 第 5 遍，输出改成一句话；**并把两处 `eval` 换成了 `float`**
- 卡住：两个，都解决了
  1. **同一个坑栽第二次**：切片又写成圆括号 `TempStr(0:-1)` → `SyntaxError`。
     冒号记住了，括号还没记住。记法换成「**圆括号打电话，方括号开抽屉**」
  2. 输入 `weqk` 报 `NameError: name 'weq' is not defined` —— `eval` 把 `weq` 当变量名去找。
     改成 `float` 后报错变成 `ValueError: could not convert string to float: 'weq'`，说人话了
- 搞懂的新东西：
  - **`eval` 会执行任意代码**：实测 `eval('__import__("os").getcwd()')` 真的返回了路径。
    真实代码里绝不拿它处理用户输入；教材用只是为了讲语法点
  - **输入校验是两层**：末位字符（if/else 管得了）+ 内容是否数字（要 `try/except`，第 5 周）
    → 所以输入 `abc` 也会报错而不是打印"你输错了"，因为末位是 c，第一层过了
  - **花括号 `{}` 的完整语法**：冒号前取哪个值、冒号后定格式（`{:.2f}` = 留两位）。已写成文末附录
- 状态：已解决
- 下次从哪开始：**周末 09-13 自查三条**（脱稿手敲 / 切片两题 / input 类型）。过不了就重来一周

---

## 2026-09-__ | 第_周 | 投入 __ 分钟 | 机器：家 / 单位

- 看了：（例：1.3 温度转换、1.4 语法元素分析）
- 敲了：（例：TempConvert.py，自己改成华氏→摄氏，已跑通）
- 卡住：（报错原文 / 现象，越具体越好；没卡就写"无"）
- 状态：已解决 / 待解决（待解决的必须写清楚我试过什么）
- 下次从哪开始：（例：第4周 4.1 程序的分支结构，先做课后练习第2题）

---

## 2026-09-__ | 第_周 | 投入 __ 分钟 | 机器：家 / 单位

- 看了：
- 敲了：
- 卡住：
- 状态：
- 下次从哪开始：

---

## 2026-09-__ | 第_周 | 投入 __ 分钟 | 机器：家 / 单位

- 看了：
- 敲了：
- 卡住：
- 状态：
- 下次从哪开始：
