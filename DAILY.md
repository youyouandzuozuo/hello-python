# 每天做什么

> 放仓库根目录，每天开工前扫一眼。两台电脑内容一样（git 同步）。  
> 第 1 周：2026-09-08（周二）开始，本周日 09-13 自查。
>
> **翻东西用 `Ctrl+F`**，搜这几个词最快：`进度` / `坑` / `自查` / `花括号`

---

## 一、开工三行（两台机器都要，每次开工先做）

```powershell
cd D:\cs-learning
.venv\Scripts\Activate.ps1
code .
```

在 VS Code 的终端里再敲一次（或者在左侧源代码管理面板点同步）：

```powershell
git pull
```

**确认两件事再开始**：提示符前有 `(.venv)`；pull 没报错。

忘了 `git pull` 就改文件，是两台机器冲突的头号原因。

---

## 二、单位 · 碎片时间（A 段 = 只看不敲）

- [x] 打开课程：`https://www.icourse163.org/course/BIT-268001`
- [x] 按顺序往下看，能看几段看几段（手机上装中国大学 MOOC App，排队午休都能刷）
- [x] **只看，不抄代码**

**为什么不能边看边抄**：抄的时候大脑在记字形，不是在想逻辑。抄完关掉视频什么都不记得。

想到的问题，随手记进 `LOG.md` 的「卡住」栏，晚上一起处理。

---

## 三、家里 · 整块 30–45 分钟（B 段 = 手敲）

- [ ] 手敲，不复制粘贴
- [ ] 关掉 AI 补全（Cursor 别开；VS Code 里 Copilot 相关提示一律忽略）
- [ ] 敲完 `Ctrl+S` 保存，再运行
- [ ] 跑通后 commit 一次

**第 1 周纪律**：AI 可以读你的代码、帮你查报错、解释某行什么意思——但**不能替你写**。这一周的笨功夫，是后面读别人代码、出问题不慌的本钱。

---

## 四、收工（2 分钟，当天在哪台机器收工就在哪台做）

填 `LOG.md`（复制下面的模板块）：

```
## 2026-09-__ | 第_周 | 投入 __ 分钟 | 机器：单位

- 看了：
- 敲了：
- 卡住：（报错原文 / 现象，越具体越好；没卡就写"无"）
- 状态：已解决 / 待解决
- 下次从哪开始：
```

然后三连：

```powershell
git add .
git commit -m "一句话说明今天做了什么"
git push
```

`.env` 已经在 `.gitignore` 里，`git add .` 不会把它传上去，放心用。

---

## 五、第 1 周进度（09-08 → 09-13）

### A 段 · 看（固定顺序，别跳）

- [x] 0.2 全课程内容导学（09-08）
- [x] 1.1 程序设计基本方法 —— IPO（09-08）
- [x] 1.3 实例 1：温度转换
- [x] 1.4 Python 程序语法元素分析

> 1.2 环境配置跳过，阶段 0 已经做过了。

### A 段 · 下一节（B 段 5 遍敲完后再开）

- [ ] **2.1 深入理解 Python 语言**（约 25 分钟，精看）
- [ ] ~~2.2 蟒蛇绘制~~ / ~~2.3 turtle 库~~ / ~~2.4 turtle 语法元素~~ —— **全跳过**

> turtle 是画小乌龟的库，后面做数据处理、调 API、RAG 全用不上，跳过不亏。
> 2.1 是纯概念课（语言分类、Python 特点），不敲代码，碎片时间看正好；
> **看到 2.1 结束就停**，2.2 开始全是 turtle。

### B 段 · 敲（同一个程序 5 遍，每遍改一点）

- [x] 第 1 遍：照抄教材版本，跑通（09-09，`week01/TempConvert.py`）
- [x] 第 2 遍：脱稿重敲，不看书（09-09 晚·家，`week01/TempConvert_v2.py`）
- [x] 第 3 遍：输入开尔文，输出摄氏（K = ℃ + 273.15）（09-10·单位，`week01/TempConvert_v3.py`）
- [x] 第 4 遍：输出保留两位小数　　（v3 用 `{:.2f}`；v4 里 C 用 `.4f`、K 用 `.2f` 故意做对比测试）
- [x] 第 5 遍：输出改成一句话（09-10·单位，`week01/TempConvert_v4.py`）
- [x] **额外**：把两处 `eval` 换成 `float`（非法输入报错从 `NameError` 变成说人话的 `ValueError`）

本周 B 段次数（至少 4 次）：　[x]　[x]　[x]　[x]　← **4/4 达标**

**全部限制在"改数字 / 改字符串 / 改输出格式"范围内。现在不要用 if 和循环** —— 那是第 4 周的内容，提前用一定卡住。

---

## 六、必须记住的 10 个东西（背不出来就抄纸上贴显示器）

1. **缩进是语法，不是排版** —— 缩进错了程序直接报错
2. `#` 是单行注释
3. 变量命名：字母 / 数字 / 下划线 / 汉字，首字符不能是数字，**区分大小写**
4. 33 个保留字（不用背，但要知道有这张表、会查）
5. 四种基础类型：字符串 / 整数 / 浮点数 / 列表
6. **字符串序号有两套**：正向从 0 开始，反向从 −1 开始 —— 新手第一道坎
7. 切片 `[M:N]` **含头不含尾**（不含第 N 个）
8. `input()` 拿到的**永远是字符串**
9. 三个类型转换：`int()` / `float()` / `str()`
10. `print()` 格式化：f-string 或 `.format()`

---

## 七、我踩过的坑（每栽一次就往这加一条）

> 这一节比上面那 10 条值钱 —— 上面是别人总结的，下面是**你自己真错过的**。>   
> 栽一次加一条，周末自查前先过一遍这里。

### 坑 1 · `.format()` 甩到了 `print` 的括号外面（2026-09-09）

```python
print("转换后的温度是{:.2f}C").format(C)    # 错
print("转换后的温度是{:.2f}C".format(C))    # 对
```

报错：`AttributeError: 'NoneType' object has no attribute 'format'`

**为什么**：`print()` 干完活不返回任何东西（Python 里叫 `None`）。  
写到外面等于「先把空模板打印出去，再想起来填数字」——来不及了，  
而且后面那句 `.format(C)` 是在对「空气」操作。

**记法**：`.format()` 永远紧贴字符串，在 `print(` 的**里面**。

---

### 坑 2 · 切片写成了小括号、中间用了逗号（2026-09-09）

> **第二次栽（09-10）**：冒号记住了，括号还是写成圆的。

```python
eval(TempStr(0,-1))    # 错
eval(TempStr[0:-1])    # 对
```

**记法**（09-10 换的，比原来好使）：

- **圆括号 `()`** = 打电话，喊函数干活：`print()` `eval()` `input()`
- **方括号 `[]`** = 开抽屉，从字符串里掏东西：`TempStr[0:-1]` `TempStr[-1]`
- 抽屉是方的、一格一格的 —— **取字符用方括号**
- 切片中间是**冒号 `:`**，不是逗号

一句话：**取字符用方括号，中间冒号，不是逗号。**

---

### 坑 3 · `if` / `elif` / `else` 后面漏了冒号（2026-09-09）

```python
if TempStr[-1] in ['F','f']       # 错，缺冒号
if TempStr[-1] in ['F','f']:      # 对
```

**记法**：这三个词后面**必须**跟 `:`，下一行缩进。  
Python 靠「冒号 + 缩进」判断哪些代码归这个条件管，没有冒号它根本读不懂。

---

### 坑 4 · 以为程序还在等输入，其实它已经跑完了（2026-09-10）

现象：跑完一次后继续敲 `2`、`2q`、`2w`，终端回 `2` 或者报  
`CommandNotFoundException: 无法将"2q"项识别为 cmdlet、函数、脚本文件或可运行程序`。

**原因**：`input()` 只收**一次**。回车之后程序继续往下执行，执行完就退出，  
提示符 `(.venv) PS D:\cs-learning>` 回来 —— **这个提示符出现 = 程序已经死了**，  
后面你敲的每一个字都是说给 PowerShell 听的，不是给 Python 听的。

**怎么分辨自己在对谁说话**：

| 提示符                          | 你在跟谁说话          | 能敲什么                   |
| ---------------------------- | --------------- | ---------------------- |
| `>>>`                        | Python 交互模式     | Python 代码              |
| （停在 input 提示语后，没有提示符）        | 正在运行的 Python 程序 | 程序要的数据                 |
| `(.venv) PS D:\cs-learning>` | PowerShell      | 系统命令，如 `python xxx.py` |

**想再测一次，必须重新跑**：

```powershell
python week01/TempConvert_v3.py
```

省事的做法：按 **`↑` 方向键**调出上一条命令，回车。

第 1 周不许用循环，所以程序没法"一直等在那里让你反复输入"——  
想试多个值就只能一次次重跑，这很正常。

---

### 坑 5 · `eval` 把非法输入当成变量名，报 `NameError`（2026-09-10）

输入 `weqk`，报错：

```
C = eval(TempStr[0:-1]) - 273.15
NameError: name 'weq' is not defined
```

**为什么**：`eval` 把字符串当**代码**执行。`weq` 不是数字，
Python 就把它当成一个**变量名**去找，找不到 → `NameError`。

**改法：把 `eval` 换成 `float`**（第 4 行和第 7 行都要改）

```python
C = eval(TempStr[0:-1]) - 273.15     # 改前
C = float(TempStr[0:-1]) - 273.15    # 改后
```

改完再输 `weqk`，报错变成：

```
ValueError: could not convert string to float: 'weq'
```

这句人话是「`weq` 转不成数字」——**原因直接写在报错里了**。

**但改完照样会崩**，只是崩得明白。要做到「输入错了就打印『输入格式错误』」，
必须用 `try / except`，那是**第 5 周**的内容，现在别碰。

**`eval` 真正的危险**（实测过）：

```python
eval('__import__("os").getcwd()')    # 真的执行了，返回 D:\cs-learning
```

换成删文件的命令就出事了。所以**真实代码里绝不拿 `eval` 处理用户输入**，
教材用它只是为了讲这个语法点。

**输入校验是两层，别混为一谈**：

| 层 | 校验什么 | 现在能处理吗 |
|---|---|---|
| 第一层 | 末位字符是不是 `K` / `C` | 能，`if / elif / else` 已经做了 |
| 第二层 | 前面那段是不是数字 | 不能，要 `float()` + `try / except` |

`weqk` 末位是 `k`，**第一层过了**，所以才会走进 if 分支、然后在第二层炸掉。
`else` 只管末位字符，管不了内容是不是数字。

---

### 坑 6 · 字符串没转数字就直接运算（2026-09-11，**同一条主线栽第三次**）

脱稿写 v5 时第 7 行写成这样：

```python
F = eval(TempStr[0:-1]*1.8+32)     # 崩
TypeError: can't multiply sequence by non-int of type 'float'
```

**为什么崩**：`TempStr[0:-1]` 取出来是**字符串** `"37"`，
字符串不能乘小数，`"37" * 1.8` 直接炸。

对比第 4 行（这个是对的）：

```python
C = (eval(TempStr[0:-1])-32)/1.8   # eval 只包住切片 → 先转数字，再运算
F = eval(TempStr[0:-1])*1.8+32     # 改后：同样只包住切片
```

**关键：区别在括号的位置，不在用不用 eval。**

**这条主线已经栽三次了**：

| 次 | 日期 | 表现 |
|---|---|---|
| 坑 2 | 09-09 | 切片写成小括号 `TempStr(0:-1)` |
| 坑 5 | 09-10 | `eval` 把 `weq` 当变量名 → `NameError` |
| 坑 6 | 09-11 | 字符串没转数字就乘 1.8 → `TypeError` |

**一句话记住**：

> **方括号 `[ ]` 取出来的是字符串，必须先变成数字，才能参与算术运算。**

顺序永远是：**切片 → 转数字（`float()` / `eval()`）→ 再算**。

---

## 八、周末自查（3 条，过不了就重来一周，别往下走）

- [ ] **不看教材，从空白文件手敲温度转换并跑通**
- [ ] 说出 `"1234"[1:3]` 和 `"1234"[-1]` 分别是什么、为什么  
  → 答案：`"23"` 和 `"4"`。说不出通常是没搞清"含头不含尾"和"反向从 −1 起"
- [ ] 说出 `input()` 拿到的是什么类型，要当数字算必须先做什么  
  → 答案：字符串。必须先 `int()` 或 `float()` 转类型，否则 `"20" + 1` 会报错

---

## 九、卡住了怎么办

**卡 15 分钟没进展就停。** 把报错原文（完整复制，不要自己概括）发出来。

别自己死磕——这个阶段死磕三小时，不如问一句。

---

## 十、本周目标

**不是"学完第 1 周"，是"第 1 周的东西变成手指头上的记忆"。**

看视频谁都能看，只有手敲出来的才长在身上。

---

## 附录 · Python 保留字 35 个（2026-09-12）

> 本机 Python 3.14.7 实测：`len(keyword.kwlist)` = **35**
> **教材（嵩天）说 33 个** —— 差的就是 `async` / `await`（Python 3.7 加的，讲异步编程，你很久才用得上）

### 按用途分 8 组（3+3+5+5+5+5+5+4 = 35）

| 组 | 个数 | 保留字 |
|---|---|---|
| **三个值**（唯一大写开头） | 3 | `True` `False` `None` |
| **判断** | 3 | `if` `elif` `else` |
| **逻辑运算** | 5 | `and` `or` `not` `is` `in` |
| **循环** | 5 | `for` `while` `break` `continue` `pass` |
| **函数与类** | 5 | `def` `return` `lambda` `class` `yield` |
| **异常处理** | 5 | `try` `except` `finally` `raise` `assert` |
| **导入与作用域** | 5 | `import` `from` `as` `global` `nonlocal` |
| **其余** | 4 | `del` `with` `async` `await` |

### 计数口诀

> **三三开头，五个五，末尾四个补上数。**
> 3 + 3 + 5×5 + 4 = 35

### 三条真正管用的记法

**① 只有三个是大写开头 —— `True` `False` `None`**
其余 32 个**全是小写**。所以看到大写开头的，必然是这三个值之一。
反过来说：**你的变量名永远别用大写开头**，天然不会撞。

**② 别背表，靠颜色认**
VS Code 里保留字会**变色**（紫色/蓝色）。你起变量名的时候，如果敲完发现它**变成彩色了**，那就是撞上保留字了，改个名就行。
**见到认识就够了，不需要默写。**

**③ 真正会用到的不到 20 个**
`lambda` `yield` `global` `nonlocal` `async` `await` `assert` `del` —— 这些你现在碰不到。
第 1 周你只需要 **3 个**：`if` `elif` `else`。

### 最容易搞混的：**这些不是保留字**

`print` `input` `eval` `format` `list` `str` `int` `float` `len` `type`

它们是**内置函数**，不是保留字（实测 `keyword.iskeyword('print')` = `False`）。

**后果比保留字更阴**：

```python
print = 5          # 不报错！合法的
print("hello")     # TypeError: 'int' object is not callable
```

保留字你根本**没法**拿它当变量名（语法直接报错，立刻发现）；
内置函数却**能**被覆盖（不报错，但那个功能从此废掉，报错信息还很怪）。

**所以：宁可撞保留字，别撞内置函数名。** 撞前者当场发现，撞后者查半天。

### 随查随用

```python
import keyword
keyword.kwlist                  # 全部 35 个
keyword.iskeyword("print")      # False —— 不是保留字
keyword.softkwlist              # ['_', 'case', 'match', 'type'] 软关键字
```

**软关键字**（`match` `case` `type` `_`）只在特定语法里有特殊含义，**可以当变量名用**，别跟真保留字混为一谈。

---

## 附录 · 花括号 `{}` 语法速查（2026-09-10）

**本质**：挖坑 + 填值。冒号**前**决定填哪个值，冒号**后**决定显示成什么样。

完整顺序（8 个位置，顺序固定，不用全写）：

```
{ 0 : * > 10 , .2f }
  │   │ │  │   │  └─ 类型（f 定点小数 / d 整数 / s 字符串 / % 百分比）
  │   │ │  │   └──── 千分位
  │   │ │  └──────── 总宽度（占几格）
  │   │ └─────────── 填充物 + 对齐方向（< 左  ^ 中  > 右）
  │   └───────────── 分隔符，前面取值、后面定格式
  └───────────────── 用第几个值（从 0 开始！）
```

叠加效果（实测）：

```python
"{0:*>10,.2f}".format(1234.567)
# 1234.567 → 1234.57 → 1,234.57 → **1,234.57（总长正好 10）
```

### 冒号前 · 六种取值方式

| 写法 | 例子 | 输出 |
|---|---|---|
| `{}` 空着 | `'{} 和 {}'.format('猫','狗')` | `猫 和 狗` |
| `{0}` `{1}` | `'{1} 和 {0}'.format('猫','狗')` | `狗 和 猫` |
| 复用同一个 | `'{0}{0}{1}'.format('咔','嚓')` | `咔咔嚓` |
| `{name}` | `'{a} 比 {b} 大'.format(a=5,b=3)` | `5 比 3 大` |
| `{0[1]}` 列表 | `'第二个是 {0[1]}'.format([10,20,30])` | `第二个是 20` |
| `{0[name]}` 字典 | `'姓名 {0[name]}'.format({'name':'张三'})` | `姓名 张三` |

### 冒号后 · 常用格式

宽度对齐（`[]` 只为显示空格，实际没有）：

```python
"[{:10}]".format("猫")      # [猫         ]  字符串默认左对齐
"[{:>10}]".format("猫")     # [         猫]  > 右对齐
"[{:^10}]".format("猫")     # [    猫     ]  ^ 居中
"[{:*^10}]".format("猫")    # [****猫*****]  * 填充
"[{:08.2f}]".format(3.14159) # [00003.14]     0 填充
```

⚠️ **数字默认右对齐，字符串默认左对齐** —— 反直觉，容易搞混。

数字格式：

| 写法 | 含义 | 输出 |
|---|---|---|
| `{:.2f}` | 保留 2 位小数 | `3.14` |
| `{:,}` | 千分位 | `1,234,567` |
| `{:+.2f}` | 强制带正负号 | `+3.14` |
| `{:.1%}` | 百分比（自动 ×100） | `85.6%` |
| `{:.2e}` | 科学计数法 | `1.23e+04` |
| `{:b}` `{:o}` `{:x}` | 二 / 八 / 十六进制 | `11111111` / `377` / `ff` |
| `{:03d}` | 整数补零到 3 位 | `007` |

**不用全背，现阶段只会用到 `{:.2f}`。** 其余的知道存在就行，用到了回来查。

### 打印真正的花括号：双写

```python
"{{}}".format()      # → {}
f"{{{a}}}"           # → {5}（a 是 5 的话）
```

### f-string · 花括号里能放表达式

第 2 周开始大量使用，比 `.format()` 短、不用数第几个值：

```python
a, b = 5, 3
f"{a} + {b} = {a+b}"                  # → 5 + 3 = 8
f"{a/b:.3f}"                          # → 1.667
f"{name.upper()} 有 {len(name)} 个字母"  # → TOM 有 3 个字母
f"{a=}"                               # → a=5   ← 调试神器，省一半字
```

### 四个常见报错

```python
"{} 和 {}".format("猫")     # IndexError: Replacement index 1 out of range ← 值给少了
"{2}".format(1, 2)         # IndexError: Replacement index 2 out of range ← 序号越界
"{name}".format(age=18)    # KeyError: 'name'                            ← 名字打错
"{}".format(1, 2)          # 不报错！多余的静静忽略 → 1                    ← 值给多了
```

第四个最阴 —— **它不报错，只是结果不对**。输出少了个数字时，先数坑和值是不是一样多。
